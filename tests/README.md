# Tests

The project currently uses the standard-library validator at `scripts/validate_project.py` as its first integrity test. Additional tests should be added when the tracker gains importers, normalized legal provisions, or generated views.

